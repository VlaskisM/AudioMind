---
phase: 05-backend-api
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - llm_analysis_service/src/web/routes/transcript.py
  - llm_analysis_service/src/web/schemas/transcript.py
  - llm_analysis_service/src/web/web.py
  - llm_analysis_service/src/configs/cors.py
  - transcription_service/src/app.py
  - transcription_service/src/configs/callback.py
  - dialogue_detection/src/app.py
  - dialogue_detection/src/configs/callback.py
  - docker-compose.yml
autonomous: true
requirements:
  - BAPI-02
  - BAPI-05
  - BAPI-06

must_haves:
  truths:
    - "GET /analysis/recordings/{id}/transcript возвращает плоский список сегментов (speaker + start + end + text) из MongoDB"
    - "CORS разрешает запросы с http://localhost:5173 к llm_analysis_service"
    - "transcription_service ставит статус transcribing при начале обработки и diarizing при успехе"
    - "dialogue_detection ставит статус ready при успехе"
    - "Оба воркера ставят статус failed при ошибке"
  artifacts:
    - path: "llm_analysis_service/src/web/routes/transcript.py"
      provides: "GET /analysis/recordings/{recording_id}/transcript endpoint"
      contains: "get_transcript"
    - path: "transcription_service/src/app.py"
      provides: "HTTP callbacks to data_ingress for transcribing/diarizing/failed statuses"
      contains: "http_client.patch"
    - path: "dialogue_detection/src/app.py"
      provides: "HTTP callbacks to data_ingress for ready/failed statuses"
      contains: "http_client.patch"
  key_links:
    - from: "llm_analysis_service/src/web/routes/transcript.py"
      to: "llm_analysis_service/src/db/mongodb/diarization_reader.py"
      via: "DiarizationReader.get_speakers()"
      pattern: "diarization_reader\\.get_speakers"
    - from: "transcription_service/src/app.py"
      to: "data_ingress PATCH /recordings/{id}/status"
      via: "httpx.AsyncClient"
      pattern: "http_client\\.patch.*status"
    - from: "dialogue_detection/src/app.py"
      to: "data_ingress PATCH /recordings/{id}/status"
      via: "httpx.AsyncClient"
      pattern: "http_client\\.patch.*status"
---

<objective>
Transcript endpoint в llm_analysis_service, CORS, и HTTP callbacks для обновления статуса из воркеров.

Purpose: Фронтенд получает диаризованную транскрипцию через API, а статус записи автоматически обновляется при прохождении каждого этапа пайплайна.
Output: GET /analysis/recordings/{id}/transcript работает. Воркеры обновляют статус записи через HTTP callback к data_ingress.
</objective>

<execution_context>
@C:/Users/Максим/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Максим/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-api/05-RESEARCH.md
@.planning/phases/05-backend-api/05-01-SUMMARY.md

<interfaces>
<!-- From llm_analysis_service (existing code) -->

From llm_analysis_service/src/db/mongodb/diarization_reader.py:
```python
class DiarizationReader:
    COLLECTION_NAME = "diarizations"
    def __init__(self, client: MongoDBClient) -> None: ...
    async def get_by_recording_id(self, recording_id: int) -> dict | None: ...
    async def get_speakers(self, recording_id: int) -> list[dict]: ...
```

Speaker format from MongoDB: `{"label": "SPEAKER_00", "segments": [{"start": 0.0, "end": 1.5, "text": "..."}]}`

From llm_analysis_service/src/web/web.py:
```python
app = FastAPI(lifespan=lifespan)
app.include_router(health_router)
app.include_router(analysis_router)
app.include_router(chat_router)
```

From llm_analysis_service/src/web/dependencies.py:
```python
def get_analysis_service(request: Request) -> AnalysisService: ...
def get_chat_service(request: Request) -> ChatService: ...
```

llm_analysis_service uses `app.state` for dependency injection (services initialized in lifespan).
DiarizationReader is already available -- it's created during lifespan and used by AnalysisService/ChatService.

From transcription_service/src/app.py:
```python
s3 = S3Downloader()
whisper = TranscriptionService()

async def process_message(body: dict) -> None:
    recording_id = body["recording_id"]
    audio_name = body["audio_name"]
    audio_path = await s3.download(audio_name)
    try:
        result = await asyncio.to_thread(whisper.transcribe, str(audio_path))
        async with UnitOfWork() as uow:
            inserted_id = await uow.transcriptions.save(recording_id, result["text"], result["segments"])
            uow.track_insert(inserted_id)
            uow.publish(recording_id, audio_name)
            await uow.commit()
    finally:
        audio_path.unlink(missing_ok=True)
```

From dialogue_detection/src/app.py:
```python
async def process_message(body: dict) -> None:
    recording_id = body["recording_id"]
    audio_name = body["audio_name"]
    audio_path = await s3.download(audio_name)
    try:
        transcription = await transcription_reader.get(recording_id)
        segments = transcription["segments"] if transcription else []
        result = await asyncio.to_thread(diarization_service.diarize, str(audio_path), segments)
        async with UnitOfWork() as uow:
            inserted_id = await uow.diarizations.save(recording_id, result["speakers"], result["num_speakers"])
            uow.track_insert(inserted_id)
            uow.publish(recording_id)
            await uow.commit()
    finally:
        audio_path.unlink(missing_ok=True)
```

Docker service names:
- data_ingress: `speechmate-app` on port 8000 (internal)
- transcription_service: `speechmate-transcription`
- dialogue_detection: `speechmate-dialogue-detection`
- llm_analysis_service: `speechmate-llm-analysis`

<!-- From 05-01 (expected output) -->
PATCH /recordings/{recording_id}/status endpoint on data_ingress accepts:
```json
{"status": "transcribing|diarizing|ready|failed", "error_message": "optional"}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcript endpoint + CORS on llm_analysis_service</name>
  <files>
    llm_analysis_service/src/web/routes/transcript.py
    llm_analysis_service/src/web/schemas/transcript.py
    llm_analysis_service/src/web/web.py
    llm_analysis_service/src/web/dependencies.py
    llm_analysis_service/src/configs/cors.py
  </files>
  <action>
    **1. Transcript schemas** (`llm_analysis_service/src/web/schemas/transcript.py`):
    ```python
    from pydantic import BaseModel

    class TranscriptSegment(BaseModel):
        speaker: str
        start: float
        end: float
        text: str

    class TranscriptResponse(BaseModel):
        recording_id: int
        segments: list[TranscriptSegment]
    ```

    **2. Dependency** (`llm_analysis_service/src/web/dependencies.py`):
    Add a dependency function to get DiarizationReader from app.state:
    ```python
    from llm_analysis_service.src.db.mongodb.diarization_reader import DiarizationReader

    def get_diarization_reader(request: Request) -> DiarizationReader:
        return request.app.state.diarization_reader
    ```
    The diarization_reader is already created in lifespan and stored in app.state (check the lifespan function in `llm_analysis_service/src/app.py` -- if diarization_reader is NOT stored in app.state, add it there). Read `llm_analysis_service/src/app.py` to verify.

    **3. Transcript route** (`llm_analysis_service/src/web/routes/transcript.py`):
    New file:
    ```python
    from fastapi import APIRouter, Depends, HTTPException

    from llm_analysis_service.src.db.mongodb.diarization_reader import DiarizationReader
    from llm_analysis_service.src.web.schemas.transcript import TranscriptResponse, TranscriptSegment
    from llm_analysis_service.src.web.dependencies import get_diarization_reader

    router = APIRouter(prefix="/analysis/recordings", tags=["transcript"])

    @router.get("/{recording_id}/transcript", response_model=TranscriptResponse)
    async def get_transcript(
        recording_id: int,
        diarization_reader: DiarizationReader = Depends(get_diarization_reader),
    ):
        """Получить диаризованную транскрипцию записи."""
        speakers = await diarization_reader.get_speakers(recording_id)
        if not speakers:
            raise HTTPException(status_code=404, detail="Transcript not found")

        segments = []
        for speaker in speakers:
            for seg in speaker["segments"]:
                segments.append(TranscriptSegment(
                    speaker=speaker["label"],
                    start=seg["start"],
                    end=seg["end"],
                    text=seg["text"],
                ))
        segments.sort(key=lambda s: s.start)

        return TranscriptResponse(recording_id=recording_id, segments=segments)
    ```

    **4. Register route** (`llm_analysis_service/src/web/web.py`):
    Add import and include the transcript router:
    ```python
    from llm_analysis_service.src.web.routes.transcript import router as transcript_router
    # ... after other include_router calls:
    app.include_router(transcript_router)
    ```

    **5. CORS config** (`llm_analysis_service/src/configs/cors.py`):
    Create new file identical in structure to the one created for data_ingress in 05-01:
    ```python
    from pathlib import Path
    from pydantic_settings import BaseSettings, SettingsConfigDict

    class CORSSettings(BaseSettings):
        CORS_ORIGINS: str = "http://localhost:5173"

        @property
        def origins_list(self) -> list[str]:
            return [o.strip() for o in self.CORS_ORIGINS.split(",") if o.strip()]

        model_config = SettingsConfigDict(
            env_file=Path(__file__).parent.parent.parent.parent / ".env",
            extra="ignore"
        )

    cors_settings = CORSSettings()
    ```

    **6. CORS middleware** (`llm_analysis_service/src/web/web.py`):
    Add CORSMiddleware BEFORE include_router calls:
    ```python
    from fastapi.middleware.cors import CORSMiddleware
    from llm_analysis_service.src.configs.cors import cors_settings

    app = FastAPI(lifespan=lifespan)

    app.add_middleware(
        CORSMiddleware,
        allow_origins=cors_settings.origins_list,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    app.include_router(health_router)
    # ... other routers
    ```
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate && python -c "from llm_analysis_service.src.web.schemas.transcript import TranscriptResponse, TranscriptSegment; print('Schemas OK')" && python -c "from llm_analysis_service.src.web.routes.transcript import router; routes = [r.path for r in router.routes]; assert '/{recording_id}/transcript' in routes; print('Route OK')"</automated>
  </verify>
  <done>GET /analysis/recordings/{id}/transcript returns flat sorted segments list. CORS middleware configured on llm_analysis_service.</done>
</task>

<task type="auto">
  <name>Task 2: Worker HTTP callbacks для обновления статуса</name>
  <files>
    transcription_service/src/app.py
    transcription_service/src/configs/callback.py
    dialogue_detection/src/app.py
    dialogue_detection/src/configs/callback.py
    docker-compose.yml
  </files>
  <action>
    **1. Add httpx dependency** to both worker services:
    - Check `transcription_service/requirements.txt` (or pyproject.toml) and `dialogue_detection/requirements.txt` -- add `httpx~=0.27` if not present. Find the dependency file first by checking what exists.

    **2. Callback config** (`transcription_service/src/configs/callback.py`):
    ```python
    from pathlib import Path
    from pydantic_settings import BaseSettings, SettingsConfigDict

    class CallbackSettings(BaseSettings):
        DATA_INGRESS_URL: str = "http://speechmate-app:8000"

        model_config = SettingsConfigDict(
            env_file=Path(__file__).parent.parent.parent.parent / ".env",
            extra="ignore"
        )

    callback_settings = CallbackSettings()
    ```
    Create identical file for `dialogue_detection/src/configs/callback.py` (adjust env_file path if needed -- check the parent chain to reach the service root `.env`).

    **3. transcription_service/src/app.py**:
    Add httpx client at module level (alongside existing module-level singletons):
    ```python
    import httpx
    from src.configs.callback import callback_settings

    http_client = httpx.AsyncClient(base_url=callback_settings.DATA_INGRESS_URL, timeout=10.0)
    ```

    Modify `process_message` to add status callbacks:
    ```python
    async def process_message(body: dict) -> None:
        recording_id = body["recording_id"]
        audio_name = body["audio_name"]

        # Статус: transcribing
        await http_client.patch(f"/recordings/{recording_id}/status", json={"status": "transcribing"})

        audio_path = await s3.download(audio_name)
        try:
            result = await asyncio.to_thread(whisper.transcribe, str(audio_path))

            async with UnitOfWork() as uow:
                inserted_id = await uow.transcriptions.save(recording_id, result["text"], result["segments"])
                uow.track_insert(inserted_id)
                uow.publish(recording_id, audio_name)
                await uow.commit()

            # Статус: diarizing (следующий этап)
            await http_client.patch(f"/recordings/{recording_id}/status", json={"status": "diarizing"})
        except Exception:
            # Статус: failed
            await http_client.patch(f"/recordings/{recording_id}/status", json={"status": "failed"})
            raise
        finally:
            audio_path.unlink(missing_ok=True)
    ```

    **4. dialogue_detection/src/app.py**:
    Add httpx client at module level:
    ```python
    import httpx
    from src.configs.callback import callback_settings

    http_client = httpx.AsyncClient(base_url=callback_settings.DATA_INGRESS_URL, timeout=10.0)
    ```

    Modify `process_message`:
    - Do NOT send "diarizing" at start (transcription_service already set it).
    - After successful `uow.commit()`, send `{"status": "ready"}`.
    - In except block, send `{"status": "failed"}`.

    ```python
    async def process_message(body: dict) -> None:
        recording_id = body["recording_id"]
        audio_name = body["audio_name"]

        audio_path = await s3.download(audio_name)
        try:
            transcription = await transcription_reader.get(recording_id)
            segments = transcription["segments"] if transcription else []

            result = await asyncio.to_thread(diarization_service.diarize, str(audio_path), segments)

            async with UnitOfWork() as uow:
                inserted_id = await uow.diarizations.save(
                    recording_id, result["speakers"], result["num_speakers"]
                )
                uow.track_insert(inserted_id)
                uow.publish(recording_id)
                await uow.commit()

            # Статус: ready
            await http_client.patch(f"/recordings/{recording_id}/status", json={"status": "ready"})
        except Exception:
            # Статус: failed
            await http_client.patch(f"/recordings/{recording_id}/status", json={"status": "failed"})
            raise
        finally:
            audio_path.unlink(missing_ok=True)
    ```

    **5. docker-compose.yml**:
    Add `DATA_INGRESS_URL` environment variable to both worker services:
    - `speechmate-transcription` environment: add `DATA_INGRESS_URL: http://speechmate-app:8000`
    - `speechmate-dialogue-detection` environment: add `DATA_INGRESS_URL: http://speechmate-app:8000`

    Also add `depends_on` for `speechmate-app` in both workers if not already present (workers need data_ingress to be reachable for callbacks).

    **Important:** Wrap the failed-status callback in a try/except to prevent masking the original error:
    ```python
    except Exception:
        try:
            await http_client.patch(f"/recordings/{recording_id}/status", json={"status": "failed"})
        except Exception:
            pass  # Don't mask original error
        raise
    ```
    Apply this pattern in BOTH workers.
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate && python -c "from transcription_service.src.configs.callback import callback_settings; assert callback_settings.DATA_INGRESS_URL; print('TS config OK')" && python -c "from dialogue_detection.src.configs.callback import callback_settings; assert callback_settings.DATA_INGRESS_URL; print('DD config OK')"</automated>
  </verify>
  <done>transcription_service sends transcribing/diarizing/failed status callbacks. dialogue_detection sends ready/failed status callbacks. Both use httpx.AsyncClient with configurable DATA_INGRESS_URL. docker-compose.yml updated with DATA_INGRESS_URL env var for both workers.</done>
</task>

</tasks>

<verification>
1. `python -c "from llm_analysis_service.src.web.routes.transcript import router"` -- transcript route importable
2. `python -c "from transcription_service.src.app import http_client"` -- httpx client exists in transcription_service
3. `python -c "from dialogue_detection.src.app import http_client"` -- httpx client exists in dialogue_detection
4. `grep -q "DATA_INGRESS_URL" docker-compose.yml` -- env var present in docker-compose
5. `grep -q "CORS_ORIGINS" llm_analysis_service/src/configs/cors.py` -- CORS config exists
</verification>

<success_criteria>
- GET /analysis/recordings/{id}/transcript возвращает плоский список сегментов, отсортированный по start time
- CORS настроен на llm_analysis_service (оба сервиса теперь с CORS)
- transcription_service отправляет PATCH status=transcribing при начале и status=diarizing при успехе
- dialogue_detection отправляет PATCH status=ready при успехе
- Оба воркера отправляют PATCH status=failed при ошибке
- httpx добавлен как зависимость воркеров
- DATA_INGRESS_URL настраивается через env
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-api/05-02-SUMMARY.md`
</output>
