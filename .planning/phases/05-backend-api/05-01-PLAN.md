---
phase: 05-backend-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data_ingress/src/db/relational/entities/recording.py
  - data_ingress/migrations/versions/add_status_and_metadata_to_recordings.py
  - data_ingress/src/repositories/recording.py
  - data_ingress/src/db/relational/repositories/recording.py
  - data_ingress/src/services/recording.py
  - data_ingress/src/web/schemas/recording.py
  - data_ingress/src/web/mappers/recording.py
  - data_ingress/src/web/routes/recording.py
  - data_ingress/src/web/web.py
  - data_ingress/src/configs/cors.py
autonomous: true
requirements:
  - BAPI-01
  - BAPI-02
  - BAPI-03
  - BAPI-04

must_haves:
  truths:
    - "GET /recordings возвращает пагинированный список записей с полями id, ts, file_url, user_id, status, original_filename"
    - "GET /recordings/{id}/status возвращает текущий статус записи"
    - "PATCH /recordings/{id}/status обновляет статус записи с валидацией переходов"
    - "CORS разрешает запросы с http://localhost:5173 к data_ingress"
  artifacts:
    - path: "data_ingress/src/db/relational/entities/recording.py"
      provides: "Recording model with status, original_filename, error_message fields"
      contains: "status"
    - path: "data_ingress/migrations/versions/add_status_and_metadata_to_recordings.py"
      provides: "Alembic migration adding status, original_filename, error_message columns"
      contains: "op.add_column"
    - path: "data_ingress/src/web/routes/recording.py"
      provides: "GET /recordings (paginated), GET /recordings/{id}/status, PATCH /recordings/{id}/status"
      contains: "get_recording_status"
  key_links:
    - from: "data_ingress/src/web/routes/recording.py"
      to: "data_ingress/src/services/recording.py"
      via: "RecordingService methods"
      pattern: "service\\.(get_recording_status|get_recordings_page|update_recording_status)"
    - from: "data_ingress/src/services/recording.py"
      to: "data_ingress/src/db/relational/repositories/recording.py"
      via: "Repository methods through UoW"
      pattern: "uow\\.recordings\\.(get_by_id|get_page|update_status)"
---

<objective>
Data_ingress: модель Recording с полем status, Alembic миграция, эндпоинты статуса и пагинированного списка, CORS.

Purpose: Подготовить data_ingress к интеграции с фронтендом -- клиент может получать статус записи, пагинированный список и обновлять статус через внутренний API.
Output: Работающие эндпоинты GET /recordings (пагинация), GET /recordings/{id}/status, PATCH /recordings/{id}/status. CORS настроен.
</objective>

<execution_context>
@C:/Users/Максим/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Максим/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-api/05-RESEARCH.md

<interfaces>
<!-- Existing code executor needs to extend -->

From data_ingress/src/db/relational/entities/recording.py:
```python
class Recording(Base):
    __tablename__ = "recordings"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    ts: Mapped[int] = mapped_column(Integer, nullable=False)
    file_url: Mapped[str] = mapped_column(String, nullable=False)
    user_id: Mapped[int] = mapped_column(Integer, nullable=False)
```

From data_ingress/src/repositories/recording.py (abstract):
```python
class AbstractRecordingRepository(ABC):
    def __init__(self, session: AsyncSession):
        self.session = session
    async def add(self, recording: Recording) -> int: ...
    async def create(self, ts: int, file_url: str, user_id: int) -> Recording: ...
    async def get_by_id(self, recording_id: int) -> Recording | None: ...
    async def get_all(self) -> list[Recording]: ...
    async def get_by_user_id(self, user_id: int) -> list[Recording]: ...
    async def delete(self, recording_id: int) -> bool: ...
```

From data_ingress/src/services/recording.py:
```python
class RecordingService:
    def __init__(self, uow_factory): ...
    async def upload_and_create_recording(self, user_id, file_obj, original_filename) -> Recording: ...
    async def create_recording(self, file_url, user_id) -> Recording: ...
    async def get_all_recordings(self) -> list[Recording]: ...
```

From data_ingress/src/web/schemas/recording.py:
```python
class RecordingCreate(BaseModel): ...
class RecordingResponse(BaseModel):
    id: int; ts: int; file_url: str; user_id: int
    model_config = {"from_attributes": True}
class RecordingListResponse(BaseModel):
    data: list[RecordingResponse]
```

From data_ingress/src/web/web.py:
```python
app = FastAPI()
app.include_router(service_router)
app.include_router(recording_router)
```

From data_ingress/src/web/mappers/recording.py:
```python
class RecordingMapper:
    @staticmethod
    def to_response(recording) -> RecordingResponse: ...
    @staticmethod
    def to_list_response(recordings) -> RecordingListResponse: ...
```

Existing Alembic migration pattern (data_ingress/migrations/versions/a1b2c3d4e5f6_remove_badge_id_from_recordings.py):
- Uses `op.add_column`, `op.drop_column`
- env.py already imports Recording model

data_ingress/src/db/uow.py exposes `self.recordings` (RecordingRepository) inside `__aenter__`.

upload_and_create_recording receives `original_filename` as parameter but does NOT save it to the Recording model currently.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recording model + Alembic migration + Repository + Service layer</name>
  <files>
    data_ingress/src/db/relational/entities/recording.py
    data_ingress/migrations/versions/add_status_and_metadata_to_recordings.py
    data_ingress/src/repositories/recording.py
    data_ingress/src/db/relational/repositories/recording.py
    data_ingress/src/services/recording.py
  </files>
  <action>
    **1. Recording model** (`data_ingress/src/db/relational/entities/recording.py`):
    Add three new fields:
    ```python
    status: Mapped[str] = mapped_column(String(20), nullable=False, server_default="uploaded")
    original_filename: Mapped[str | None] = mapped_column(String, nullable=True)
    error_message: Mapped[str | None] = mapped_column(String, nullable=True)
    ```
    Use `server_default="uploaded"` for status so existing rows get the default value.
    `original_filename` is nullable because existing rows don't have it.
    `error_message` is nullable -- only populated on failure.

    **2. Alembic migration** (`data_ingress/migrations/versions/add_status_and_metadata_to_recordings.py`):
    Create manually (do NOT run `alembic revision --autogenerate` -- we're not in Docker):
    - File name: pick a random 12-char hex revision id
    - `depends_on` = revision id from `a1b2c3d4e5f6_remove_badge_id_from_recordings.py` (read its `revision` variable)
    - `upgrade()`: `op.add_column('recordings', sa.Column('status', sa.String(20), nullable=False, server_default='uploaded'))`, `op.add_column('recordings', sa.Column('original_filename', sa.String(), nullable=True))`, `op.add_column('recordings', sa.Column('error_message', sa.String(), nullable=True))`
    - `downgrade()`: `op.drop_column('recordings', 'error_message')`, `op.drop_column('recordings', 'original_filename')`, `op.drop_column('recordings', 'status')`

    **3. Abstract repository** (`data_ingress/src/repositories/recording.py`):
    Add two abstract methods:
    ```python
    @abstractmethod
    async def get_page(self, offset: int = 0, limit: int = 20) -> tuple[list[Recording], int]: ...

    @abstractmethod
    async def update_status(self, recording_id: int, status: str, error_message: str | None = None) -> Recording | None: ...
    ```

    **4. Concrete repository** (`data_ingress/src/db/relational/repositories/recording.py`):
    Implement `get_page`: use `select(func.count()).select_from(Recording)` for total, `select(Recording).order_by(Recording.ts.desc()).offset(offset).limit(limit)` for items. Return `(items, total)`.
    Implement `update_status`: `get_by_id`, if None return None, set `recording.status = status`, if error_message set it too, `await self.session.flush()`, return recording.
    Import `func` from `sqlalchemy`.

    **5. RecordingService** (`data_ingress/src/services/recording.py`):
    - Modify `_create_recording` to accept optional `original_filename: str | None = None` and pass it to `Recording(...)`.
    - Modify `upload_and_create_recording` to pass `original_filename=original_filename` to `_create_recording`.
    - Add method:
      ```python
      async def get_recording_status(self, recording_id: int) -> Recording | None:
          async with self._uow_factory() as uow:
              return await uow.recordings.get_by_id(recording_id)
      ```
    - Add method:
      ```python
      async def get_recordings_page(self, offset: int = 0, limit: int = 20) -> tuple[list[Recording], int]:
          async with self._uow_factory() as uow:
              return await uow.recordings.get_page(offset, limit)
      ```
    - Add method:
      ```python
      async def update_recording_status(self, recording_id: int, status: str, error_message: str | None = None) -> Recording | None:
          async with self._uow_factory() as uow:
              recording = await uow.recordings.update_status(recording_id, status, error_message)
              if recording:
                  await uow.commit()
              return recording
      ```

    **Status transition validation:** In `update_status` (repository or service, pick service), validate allowed transitions:
    - `uploaded` -> `transcribing`
    - `transcribing` -> `diarizing`, `failed`
    - `diarizing` -> `ready`, `failed`
    - `failed` is terminal -- no transitions from it
    - `ready` is terminal -- no transitions from it
    Raise `ValueError` if transition is invalid.
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate/data_ingress && python -c "from src.db.relational.entities.recording import Recording; r = Recording(ts=1, file_url='x', user_id=1); assert hasattr(r, 'status'); assert hasattr(r, 'original_filename'); assert hasattr(r, 'error_message'); print('OK')"</automated>
  </verify>
  <done>Recording model has status/original_filename/error_message fields. Alembic migration file exists. Abstract and concrete repository have get_page and update_status. RecordingService has get_recording_status, get_recordings_page, update_recording_status with transition validation.</done>
</task>

<task type="auto">
  <name>Task 2: Schemas + Routes + CORS + Mapper updates</name>
  <files>
    data_ingress/src/web/schemas/recording.py
    data_ingress/src/web/mappers/recording.py
    data_ingress/src/web/routes/recording.py
    data_ingress/src/web/web.py
    data_ingress/src/configs/cors.py
  </files>
  <action>
    **1. Schemas** (`data_ingress/src/web/schemas/recording.py`):
    Update `RecordingResponse` to include new fields:
    ```python
    class RecordingResponse(BaseModel):
        id: int
        ts: int
        file_url: str
        user_id: int
        status: str
        original_filename: str | None = None

        model_config = {"from_attributes": True}
    ```

    Add new schemas:
    ```python
    class StatusResponse(BaseModel):
        status: str  # outer status = "ok"
        data: RecordingStatusData

    class RecordingStatusData(BaseModel):
        id: int
        status: str
        error_message: str | None = None

        model_config = {"from_attributes": True}

    class StatusUpdate(BaseModel):
        status: str  # one of: transcribing, diarizing, ready, failed
        error_message: str | None = None

    class PaginatedRecordingListResponse(BaseModel):
        data: list[RecordingResponse]
        total: int
        offset: int
        limit: int
    ```

    **2. Mapper** (`data_ingress/src/web/mappers/recording.py`):
    Update imports. Add methods:
    - `to_status_response(recording) -> StatusResponse` -- wrap recording into StatusResponse with RecordingStatusData.
    - `to_paginated_list_response(recordings, total, offset, limit) -> PaginatedRecordingListResponse`.
    Update imports from schemas to include new classes.

    **3. Routes** (`data_ingress/src/web/routes/recording.py`):
    Update existing `list_recordings` to use pagination:
    ```python
    @router.get("/", response_model=PaginatedRecordingListResponse)
    async def list_recordings(offset: int = 0, limit: int = 20):
        recordings, total = await service.get_recordings_page(offset, limit)
        return mapper.to_paginated_list_response(recordings, total, offset, limit)
    ```

    Add `GET /{recording_id}/status`:
    ```python
    @router.get("/{recording_id}/status", response_model=StatusResponse)
    async def get_recording_status(recording_id: int):
        recording = await service.get_recording_status(recording_id)
        if recording is None:
            raise HTTPException(status_code=404, detail="Recording not found")
        return mapper.to_status_response(recording)
    ```

    Add `PATCH /{recording_id}/status` (internal, for worker callbacks):
    ```python
    @router.patch("/{recording_id}/status", response_model=StatusResponse)
    async def update_recording_status(recording_id: int, body: StatusUpdate):
        try:
            recording = await service.update_recording_status(recording_id, body.status, body.error_message)
        except ValueError as e:
            raise HTTPException(status_code=400, detail=str(e))
        if recording is None:
            raise HTTPException(status_code=404, detail="Recording not found")
        return mapper.to_status_response(recording)
    ```

    Import `HTTPException` and new schemas. Keep existing create_recording and upload_recording routes unchanged.

    **4. CORS config** (`data_ingress/src/configs/cors.py`):
    ```python
    from pathlib import Path
    from pydantic_settings import BaseSettings, SettingsConfigDict

    class CORSSettings(BaseSettings):
        CORS_ORIGINS: str = "http://localhost:5173"

        @property
        def origins_list(self) -> list[str]:
            return [o.strip() for o in self.CORS_ORIGINS.split(",") if o.strip()]

        model_config = SettingsConfigDict(
            env_file=Path(__file__).parent.parent.parent.parent / ".env",
            extra="ignore"
        )

    cors_settings = CORSSettings()
    ```

    **5. data_ingress web.py** (`data_ingress/src/web/web.py`):
    Add CORS middleware BEFORE including routers:
    ```python
    from fastapi import FastAPI
    from fastapi.middleware.cors import CORSMiddleware
    from src.configs.cors import cors_settings
    from src.web.routes.health_check import router as service_router
    from src.web.routes.recording import router as recording_router

    app = FastAPI()

    app.add_middleware(
        CORSMiddleware,
        allow_origins=cors_settings.origins_list,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    app.include_router(service_router)
    app.include_router(recording_router)
    ```
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate/data_ingress && python -c "from src.web.web import app; routes = [r.path for r in app.routes]; assert '/recordings/{recording_id}/status' in routes; assert '/recordings/' in routes; print('Routes OK'); mw_types = [type(m).__name__ for m in app.user_middleware]; print('Middleware:', mw_types)"</automated>
  </verify>
  <done>RecordingResponse includes status and original_filename. GET /recordings returns paginated list. GET /recordings/{id}/status returns status. PATCH /recordings/{id}/status updates status. CORS configured on data_ingress via CORSMiddleware with configurable origins.</done>
</task>

</tasks>

<verification>
1. `cd data_ingress && python -c "from src.db.relational.entities.recording import Recording; assert hasattr(Recording, 'status')"` -- модель имеет поле status
2. `cd data_ingress && python -c "from src.web.web import app; print([r.path for r in app.routes])"` -- все три новых маршрута присутствуют
3. Alembic migration file exists in `data_ingress/migrations/versions/` with `add_column` operations
4. CORS middleware present in app middleware stack
</verification>

<success_criteria>
- Recording model имеет поля status, original_filename, error_message
- Alembic миграция создана с server_default='uploaded' для status
- GET /recordings возвращает PaginatedRecordingListResponse с total/offset/limit
- GET /recordings/{id}/status возвращает текущий статус
- PATCH /recordings/{id}/status обновляет статус с валидацией переходов (uploaded->transcribing->diarizing->ready/failed)
- CORSMiddleware настроен на data_ingress
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-api/05-01-SUMMARY.md`
</output>
