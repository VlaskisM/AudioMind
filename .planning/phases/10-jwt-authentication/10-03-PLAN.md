---
phase: 10-jwt-authentication
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - frontend/src/stores/authStore.ts
  - frontend/src/api/auth.ts
  - frontend/src/api/ingress.ts
  - frontend/src/api/analysis.ts
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/RegisterPage.tsx
  - frontend/src/components/ProtectedRoute.tsx
  - frontend/src/router.tsx
autonomous: false
requirements: [AUTH-07, AUTH-08, AUTH-09]

must_haves:
  truths:
    - "Пользователь видит форму логина на /login"
    - "Пользователь может перейти на /register и зарегистрироваться"
    - "После успешного логина пользователь попадает на главную страницу"
    - "Токен сохраняется в localStorage и автоматически подставляется в запросы"
    - "При отсутствии токена любой защищенный маршрут перенаправляет на /login"
    - "При 401 ответе от API пользователь перенаправляется на /login"
  artifacts:
    - path: "frontend/src/stores/authStore.ts"
      provides: "Zustand store с token, user, setAuth, logout"
      contains: "useAuthStore"
    - path: "frontend/src/api/auth.ts"
      provides: "login() и register() API функции"
      contains: "login"
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Форма логина с email и password"
      contains: "LoginPage"
    - path: "frontend/src/pages/RegisterPage.tsx"
      provides: "Форма регистрации"
      contains: "RegisterPage"
    - path: "frontend/src/components/ProtectedRoute.tsx"
      provides: "Wrapper для защищенных маршрутов"
      contains: "ProtectedRoute"
  key_links:
    - from: "frontend/src/api/ingress.ts"
      to: "frontend/src/stores/authStore.ts"
      via: "axios request interceptor подставляет Bearer token"
      pattern: "interceptors\\.request\\.use"
    - from: "frontend/src/api/analysis.ts"
      to: "frontend/src/stores/authStore.ts"
      via: "axios request interceptor подставляет Bearer token"
      pattern: "interceptors\\.request\\.use"
    - from: "frontend/src/components/ProtectedRoute.tsx"
      to: "frontend/src/stores/authStore.ts"
      via: "Проверка наличия token"
      pattern: "useAuthStore"
    - from: "frontend/src/router.tsx"
      to: "frontend/src/components/ProtectedRoute.tsx"
      via: "Оборачивание маршрутов"
      pattern: "ProtectedRoute"
---

<objective>
Реализовать фронтенд аутентификации: zustand auth store, axios interceptors для Authorization header, страницы логина/регистрации, ProtectedRoute для защиты маршрутов.

Purpose: Без фронтенд-части пользователь не может войти в систему и все API-запросы возвращают 401.
Output: Работающий flow регистрации и логина, автоматическая подстановка токена, редирект на /login при отсутствии авторизации.
</objective>

<execution_context>
@C:/Users/Максим/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Максим/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-jwt-authentication/10-RESEARCH.md
@.planning/phases/10-jwt-authentication/10-01-SUMMARY.md
@.planning/phases/10-jwt-authentication/10-02-SUMMARY.md

<interfaces>
<!-- Существующий фронтенд-код -->

From frontend/src/api/ingress.ts:
```typescript
import axios from 'axios'

export const ingressApi = axios.create({
  baseURL: '/api/ingress',
})

export function uploadRecording(file: File, onProgress?) {
  // Текущий вызов: '/recordings/upload?user_id=1' — НУЖНО УБРАТЬ ?user_id=1
  return ingressApi.post<RecordingResponse>('/recordings/upload?user_id=1', formData, ...)
}
```

From frontend/src/api/analysis.ts:
```typescript
import axios from 'axios'

export const analysisApi = axios.create({
  baseURL: '/api/analysis',
})
```

From frontend/src/stores/workspaceStore.ts:
```typescript
// Zustand store паттерн проекта
import { create } from 'zustand'
```

From frontend/src/router.tsx:
```typescript
import { createBrowserRouter } from 'react-router'
export const router = createBrowserRouter([
  { path: '/', Component: UploadPage },
  { path: '/recordings/:id/processing', Component: ProcessingPage },
  { path: '/recordings/:id', Component: WorkspacePage },
])
```

From frontend/src/main.tsx:
```typescript
import { RouterProvider } from 'react-router'
import { router } from './router'
// Uses RouterProvider — стандартный react-router v7 data router
```

Backend auth endpoints (из плана 10-01):
- POST /api/ingress/auth/register — body: {email, password}, response: {id, email}
- POST /api/ingress/auth/login — body: {email, password}, response: {access_token, token_type}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth store, API, interceptors, страницы логина/регистрации</name>
  <files>
    frontend/src/stores/authStore.ts
    frontend/src/api/auth.ts
    frontend/src/api/ingress.ts
    frontend/src/api/analysis.ts
    frontend/src/pages/LoginPage.tsx
    frontend/src/pages/RegisterPage.tsx
  </files>
  <action>
1. Создать `frontend/src/stores/authStore.ts` — zustand store с persist middleware:
   ```typescript
   import { create } from 'zustand'
   import { persist } from 'zustand/middleware'

   interface AuthUser {
     id: number
     email: string
   }

   interface AuthState {
     token: string | null
     user: AuthUser | null
     setAuth: (token: string, user: AuthUser) => void
     logout: () => void
   }

   export const useAuthStore = create<AuthState>()(
     persist(
       (set) => ({
         token: null,
         user: null,
         setAuth: (token, user) => set({ token, user }),
         logout: () => set({ token: null, user: null }),
       }),
       { name: 'auth-storage' }
     )
   )
   ```

2. Создать `frontend/src/api/auth.ts` — API-функции для auth:
   ```typescript
   import { ingressApi } from './ingress'

   interface LoginResponse { access_token: string; token_type: string }
   interface RegisterResponse { id: number; email: string }

   export function login(email: string, password: string) {
     return ingressApi.post<LoginResponse>('/auth/login', { email, password })
   }

   export function register(email: string, password: string) {
     return ingressApi.post<RegisterResponse>('/auth/register', { email, password })
   }
   ```

3. Обновить `frontend/src/api/ingress.ts`:
   - Добавить request interceptor для Authorization header:
     ```typescript
     import { useAuthStore } from '../stores/authStore'

     ingressApi.interceptors.request.use((config) => {
       const token = useAuthStore.getState().token
       if (token) {
         config.headers.Authorization = `Bearer ${token}`
       }
       return config
     })
     ```
   - Добавить response interceptor для обработки 401:
     ```typescript
     ingressApi.interceptors.response.use(
       (response) => response,
       (error) => {
         if (error.response?.status === 401) {
           useAuthStore.getState().logout()
           window.location.href = '/login'
         }
         return Promise.reject(error)
       }
     )
     ```
   - КРИТИЧНО: Убрать `?user_id=1` из `uploadRecording` — user_id теперь берется из JWT на бэкенде. Строку `/recordings/upload?user_id=1` заменить на `/recordings/upload`.

4. Обновить `frontend/src/api/analysis.ts`:
   - Добавить аналогичные request и response interceptors для analysisApi:
     ```typescript
     import { useAuthStore } from '../stores/authStore'

     analysisApi.interceptors.request.use((config) => {
       const token = useAuthStore.getState().token
       if (token) {
         config.headers.Authorization = `Bearer ${token}`
       }
       return config
     })

     analysisApi.interceptors.response.use(
       (response) => response,
       (error) => {
         if (error.response?.status === 401) {
           useAuthStore.getState().logout()
           window.location.href = '/login'
         }
         return Promise.reject(error)
       }
     )
     ```

5. Создать `frontend/src/pages/LoginPage.tsx`:
   - Форма с полями email и password
   - Кнопка "Войти"
   - Ссылка "Нет аккаунта? Зарегистрироваться" -> /register
   - При submit: вызвать login() API, при успехе: вызвать setAuth(token, user) из authStore, navigate('/')
   - При ошибке: показать текст ошибки под формой
   - Использовать useState для email, password, error, loading
   - Использовать useNavigate() из react-router
   - Если пользователь уже авторизован (token !== null) — Navigate to '/'
   - Минимальный стиль: центрирование формы на странице, Tailwind если используется, иначе inline styles / CSS modules. Проверить что проект использует — посмотреть на существующие страницы.

6. Создать `frontend/src/pages/RegisterPage.tsx`:
   - Аналогичная форма с email и password
   - Кнопка "Зарегистрироваться"
   - Ссылка "Уже есть аккаунт? Войти" -> /login
   - При submit: вызвать register() API, при успехе: автоматически вызвать login(), затем setAuth и navigate('/')
   - При ошибке: показать текст ошибки
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - authStore экспортирует useAuthStore с token, user, setAuth, logout
    - auth.ts содержит login() и register() функции
    - ingressApi и analysisApi автоматически подставляют Bearer token
    - ingressApi и analysisApi перенаправляют на /login при 401
    - uploadRecording не содержит ?user_id=1
    - LoginPage отображает форму логина
    - RegisterPage отображает форму регистрации
  </done>
</task>

<task type="auto">
  <name>Task 2: ProtectedRoute + обновление router</name>
  <files>
    frontend/src/components/ProtectedRoute.tsx
    frontend/src/router.tsx
  </files>
  <action>
1. Создать `frontend/src/components/ProtectedRoute.tsx`:
   ```typescript
   import { Navigate } from 'react-router'
   import { useAuthStore } from '../stores/authStore'

   export function ProtectedRoute({ children }: { children: React.ReactNode }) {
     const token = useAuthStore((s) => s.token)
     if (!token) return <Navigate to="/login" replace />
     return <>{children}</>
   }
   ```

2. Обновить `frontend/src/router.tsx`:
   - Добавить импорты: LoginPage, RegisterPage, ProtectedRoute
   - Добавить маршруты:
     ```typescript
     { path: '/login', Component: LoginPage },
     { path: '/register', Component: RegisterPage },
     ```
   - Обернуть все существующие маршруты в ProtectedRoute:
     ```typescript
     {
       path: '/',
       element: <ProtectedRoute><UploadPage /></ProtectedRoute>,
     },
     {
       path: '/recordings/:id/processing',
       element: <ProtectedRoute><ProcessingPage /></ProtectedRoute>,
     },
     {
       path: '/recordings/:id',
       element: <ProtectedRoute><WorkspacePage /></ProtectedRoute>,
     },
     ```
   - ВАЖНО: Использовать `element` вместо `Component` для маршрутов с ProtectedRoute wrapper, чтобы можно было обернуть в JSX.
   - /login и /register НЕ оборачивать в ProtectedRoute.
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - ProtectedRoute проверяет наличие token и редиректит на /login
    - /login и /register доступны без авторизации
    - Все остальные маршруты обернуты в ProtectedRoute
    - TypeScript компиляция проходит без ошибок
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Верификация полного auth flow</name>
  <action>
    Полный стек JWT-аутентификации: бэкенд (регистрация, логин, защита эндпоинтов, фильтрация по user_id) и фронтенд (формы логина/регистрации, автоматическая подстановка токена, редирект).
    Пересобрать docker compose и проверить end-to-end flow.
  </action>
  <how-to-verify>
    1. Пересобрать и запустить: `docker compose up --build -d`
    2. Дождаться healthcheck postgres, затем запустить миграцию: `docker compose exec app alembic upgrade head`
    3. Открыть http://localhost — должен редиректить на /login
    4. Перейти на /register, зарегистрировать нового пользователя (email + password)
    5. После регистрации должен автоматически перейти на главную страницу
    6. Загрузить аудиофайл — должен успешно загрузиться (user_id из JWT)
    7. Выйти (очистить localStorage или добавить кнопку logout), зайти другим пользователем — старые записи не видны
    8. Попробовать напрямую GET http://localhost/api/ingress/recordings/ без Authorization header — должен вернуть 401
    9. Проверить что PATCH http://localhost/api/ingress/recordings/1/status с body {"status":"transcribing"} работает без JWT
  </how-to-verify>
  <verify>Ручная верификация пользователем</verify>
  <done>Полный auth flow работает: регистрация, логин, защита эндпоинтов, изоляция данных</done>
  <resume-signal>Напишите "approved" или опишите проблемы</resume-signal>
</task>

</tasks>

<verification>
- Неавторизованный пользователь видит /login
- Регистрация создает пользователя и автоматически входит
- После логина все API-запросы содержат Authorization: Bearer token
- Защищенные маршруты редиректят на /login без токена
- 401 от API очищает токен и редиректит на /login
- uploadRecording не передает ?user_id=1
</verification>

<success_criteria>
- Полный flow: регистрация -> автоматический вход -> загрузка файла -> просмотр анализа
- Изоляция данных между пользователями
- Worker callbacks продолжают работать без JWT
</success_criteria>

<output>
After completion, create `.planning/phases/10-jwt-authentication/10-03-SUMMARY.md`
</output>
