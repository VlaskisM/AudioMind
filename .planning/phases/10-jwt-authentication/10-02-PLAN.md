---
phase: 10-jwt-authentication
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - data_ingress/src/web/routes/recording.py
  - data_ingress/src/services/recording.py
  - data_ingress/src/db/relational/repositories/recording.py
  - data_ingress/src/repositories/recording.py
  - llm_analysis_service/src/configs/auth.py
  - llm_analysis_service/src/web/dependencies/__init__.py
  - llm_analysis_service/src/web/dependencies/auth.py
  - llm_analysis_service/src/web/routes/analysis.py
  - llm_analysis_service/src/web/routes/chat.py
  - llm_analysis_service/src/web/routes/transcript.py
  - llm_analysis_service/requirements.txt
  - nginx/nginx.conf
autonomous: true
requirements: [AUTH-03, AUTH-04, AUTH-05, AUTH-06]

must_haves:
  truths:
    - "GET /recordings/ без JWT возвращает 401"
    - "POST /recordings/upload без JWT возвращает 401"
    - "PATCH /recordings/{id}/status работает БЕЗ JWT (internal callback)"
    - "GET /recordings/ с JWT возвращает только записи текущего user_id"
    - "POST /recordings/upload с JWT привязывает запись к user_id из токена"
    - "POST /analysis/{id}/summary без JWT возвращает 401"
    - "POST /analysis/{id}/chat без JWT возвращает 401"
  artifacts:
    - path: "data_ingress/src/web/routes/recording.py"
      provides: "Защищенные recording-эндпоинты с user_id из JWT"
      contains: "get_current_user"
    - path: "llm_analysis_service/src/web/dependencies/auth.py"
      provides: "JWT-валидация в llm_analysis_service"
      contains: "get_current_user"
    - path: "llm_analysis_service/src/configs/auth.py"
      provides: "AuthSettings с JWT_SECRET"
      contains: "AuthSettings"
  key_links:
    - from: "data_ingress/src/web/routes/recording.py"
      to: "data_ingress/src/web/dependencies/auth.py"
      via: "Depends(get_current_user)"
      pattern: "Depends\\(get_current_user\\)"
    - from: "data_ingress/src/services/recording.py"
      to: "data_ingress/src/db/relational/repositories/recording.py"
      via: "get_page(offset, limit, user_id=user_id)"
      pattern: "user_id"
    - from: "llm_analysis_service/src/web/routes/analysis.py"
      to: "llm_analysis_service/src/web/dependencies/auth.py"
      via: "Depends(get_current_user)"
      pattern: "Depends\\(get_current_user\\)"
---

<objective>
Защитить все эндпоинты JWT-авторизацией: recordings в data_ingress (кроме internal status callback), все analysis/chat/transcript в llm_analysis_service. Фильтрация записей по user_id из токена, привязка upload к user_id из JWT.

Purpose: Без этого любой может получить доступ к записям и анализам любого пользователя.
Output: Все публичные эндпоинты требуют JWT, внутренний PATCH status открыт для worker-сервисов.
</objective>

<execution_context>
@C:/Users/Максим/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Максим/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-jwt-authentication/10-RESEARCH.md
@.planning/phases/10-jwt-authentication/10-01-SUMMARY.md

<interfaces>
<!-- Из плана 10-01: auth dependency, созданный в предыдущем плане -->

From data_ingress/src/web/dependencies/auth.py (создан в 10-01):
```python
from typing import Annotated
from fastapi import Depends
from fastapi.security import OAuth2PasswordBearer

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/auth/login")

async def get_current_user(
    token: Annotated[str, Depends(oauth2_scheme)],
) -> int:
    """Возвращает user_id (int) из JWT токена. Raises 401 если невалиден."""
    ...
```

From data_ingress/src/web/routes/recording.py (текущий код — модифицировать):
```python
router = APIRouter(prefix="/recordings", tags=["recordings"])

@router.get("/", response_model=PaginatedRecordingListResponse)
async def list_recordings(offset: int = 0, limit: int = 20): ...

@router.post("/upload", status_code=status.HTTP_201_CREATED, response_model=RecordingResponse)
async def upload_recording(user_id: int, file: UploadFile = File(...)): ...

@router.patch("/{recording_id}/status", response_model=StatusResponse)
async def update_recording_status(recording_id: int, body: StatusUpdate): ...
```

From data_ingress/src/services/recording.py (текущий код):
```python
class RecordingService:
    async def upload_and_create_recording(self, user_id: int, file_obj, original_filename): ...
    async def get_recordings_page(self, offset: int = 0, limit: int = 20) -> tuple[list[Recording], int]: ...
```

From data_ingress/src/db/relational/repositories/recording.py (текущий код):
```python
class RecordingRepository:
    async def get_page(self, offset: int = 0, limit: int = 20) -> tuple[list[Recording], int]: ...
    async def get_by_user_id(self, user_id: int) -> list[Recording]: ...
```

From llm_analysis_service/src/web/routes/analysis.py (текущий код):
```python
router = APIRouter(prefix="/analysis", tags=["analysis"])
@router.post("/{recording_id}/summary", response_model=SummaryResponse)
async def get_summary(recording_id: int, service = Depends(get_analysis_service)): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Защита data_ingress recordings + фильтрация по user_id</name>
  <files>
    data_ingress/src/web/routes/recording.py
    data_ingress/src/services/recording.py
    data_ingress/src/db/relational/repositories/recording.py
    data_ingress/src/repositories/recording.py
  </files>
  <action>
1. Обновить `data_ingress/src/repositories/recording.py` (абстрактный):
   - Изменить сигнатуру `get_page`: добавить опциональный параметр `user_id: int | None = None`

2. Обновить `data_ingress/src/db/relational/repositories/recording.py`:
   - Изменить `get_page(self, offset, limit, user_id=None)`: если user_id не None, добавить `.where(Recording.user_id == user_id)` к обоим запросам (count и items)

3. Обновить `data_ingress/src/services/recording.py`:
   - Изменить `get_recordings_page(self, offset, limit, user_id=None)`: передать user_id в `uow.recordings.get_page(offset, limit, user_id=user_id)`
   - Метод `upload_and_create_recording` уже принимает user_id — оставить без изменений

4. Обновить `data_ingress/src/web/routes/recording.py`:
   - Добавить import: `from typing import Annotated`, `from fastapi import Depends`, `from src.web.dependencies.auth import get_current_user`
   - Изменить `list_recordings`: добавить `user_id: Annotated[int, Depends(get_current_user)]`, передать user_id в service.get_recordings_page(offset, limit, user_id=user_id)
   - Изменить `upload_recording`: ЗАМЕНИТЬ `user_id: int` query param на `user_id: Annotated[int, Depends(get_current_user)]`, удалить user_id из query params
   - Изменить `create_recording`: добавить `user_id: Annotated[int, Depends(get_current_user)]`, заменить body.user_id на user_id из JWT
   - Изменить `get_recording_status`: добавить `user_id: Annotated[int, Depends(get_current_user)]` (защитить доступ, но пока не фильтровать по user_id — только требуем авторизацию)
   - **КРИТИЧНО:** `update_recording_status` (PATCH /{recording_id}/status) НЕ добавлять get_current_user — это internal endpoint для worker-сервисов
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate && python -c "
from data_ingress.src.web.routes.recording import router
for route in router.routes:
    deps = [d.dependency.__name__ for d in getattr(route, 'dependant', type('',(),{'dependencies':[]})()).dependencies] if hasattr(route, 'dependant') else []
    print(f'{route.methods} {route.path} deps={deps}')
" 2>&1 || echo "Import check — verify manually after docker rebuild"</automated>
  </verify>
  <done>
    - GET /recordings/ требует JWT и фильтрует по user_id из токена
    - POST /recordings/upload берет user_id из JWT (не из query param)
    - POST /recordings/ берет user_id из JWT
    - GET /recordings/{id}/status требует JWT
    - PATCH /recordings/{id}/status работает БЕЗ JWT
  </done>
</task>

<task type="auto">
  <name>Task 2: JWT-валидация в llm_analysis_service + nginx auth route</name>
  <files>
    llm_analysis_service/src/configs/auth.py
    llm_analysis_service/src/web/dependencies/__init__.py
    llm_analysis_service/src/web/dependencies/auth.py
    llm_analysis_service/src/web/routes/analysis.py
    llm_analysis_service/src/web/routes/chat.py
    llm_analysis_service/src/web/routes/transcript.py
    llm_analysis_service/requirements.txt
    nginx/nginx.conf
  </files>
  <action>
1. Добавить `pyjwt>=2.8.0` в `llm_analysis_service/requirements.txt`.

2. Создать `llm_analysis_service/src/configs/auth.py` — AuthSettings (только валидация, не создание):
   ```python
   from pathlib import Path
   from pydantic_settings import BaseSettings, SettingsConfigDict

   class AuthSettings(BaseSettings):
       JWT_SECRET: str
       JWT_ALGORITHM: str = "HS256"

       model_config = SettingsConfigDict(
           env_file=Path(__file__).parent.parent.parent.parent / ".env",
           extra="ignore",
       )

   auth_settings = AuthSettings()
   ```
   ВАЖНО: env_file путь может отличаться от data_ingress — проверить количество parent'ов до корня проекта. llm_analysis_service/src/configs/auth.py -> 4 parent'а до llm_analysis_service/, но .env в корне Speechmate/. Если сервис запускается в Docker с env_file в docker-compose.yml, то .env читается из env vars окружения — SettingsConfigDict extra="ignore" позволяет это. Убедиться что env_file путь корректен ИЛИ просто положиться на env vars из docker-compose.

3. Создать `llm_analysis_service/src/web/dependencies/` директорию с пустым `__init__.py`.

4. Создать `llm_analysis_service/src/web/dependencies/auth.py` — аналог data_ingress:
   ```python
   from typing import Annotated
   import jwt
   from jwt.exceptions import InvalidTokenError
   from fastapi import Depends, HTTPException, status
   from fastapi.security import OAuth2PasswordBearer
   from src.configs.auth import auth_settings

   oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/ingress/auth/login")

   async def get_current_user(
       token: Annotated[str, Depends(oauth2_scheme)],
   ) -> int:
       credentials_exception = HTTPException(
           status_code=status.HTTP_401_UNAUTHORIZED,
           detail="Could not validate credentials",
           headers={"WWW-Authenticate": "Bearer"},
       )
       try:
           payload = jwt.decode(token, auth_settings.JWT_SECRET, algorithms=[auth_settings.JWT_ALGORITHM])
           user_id: int | None = payload.get("sub")
           if user_id is None:
               raise credentials_exception
       except InvalidTokenError:
           raise credentials_exception
       return user_id
   ```
   ВАЖНО: tokenUrl="/api/ingress/auth/login" — путь через nginx, чтобы Swagger UI мог найти login endpoint.

5. Обновить `llm_analysis_service/src/web/routes/analysis.py`:
   - Добавить import: `from typing import Annotated`, `from fastapi import Depends`, `from src.web.dependencies.auth import get_current_user`
   - Добавить `user_id: Annotated[int, Depends(get_current_user)]` ко ВСЕМ 4 эндпоинтам (summary, key-points, action-items, faq). Параметр user_id пока не используется в логике (ownership-проверку отложить) — просто требуем авторизацию.

6. Обновить `llm_analysis_service/src/web/routes/chat.py`:
   - Аналогично: добавить get_current_user dependency ко всем эндпоинтам.

7. Обновить `llm_analysis_service/src/web/routes/transcript.py`:
   - Аналогично: добавить get_current_user dependency ко всем эндпоинтам.

8. Обновить `nginx/nginx.conf`: добавить location для `/api/ingress/auth/` чтобы auth-эндпоинты были доступны через nginx. Проверить что текущий location `/api/ingress/` уже покрывает `/api/ingress/auth/` — если да, никаких изменений не нужно. Nginx location prefix matching: `/api/ingress/` уже перенаправит `/api/ingress/auth/login` на upstream data_ingress. Скорее всего изменений в nginx не нужно, но ПРОВЕРИТЬ.
  </action>
  <verify>
    <automated>cd E:/VisualStudioCodeProjects/Speechmate && python -c "from llm_analysis_service.src.configs.auth import auth_settings; print('LLM Auth OK')" && python -c "from llm_analysis_service.src.web.dependencies.auth import get_current_user; print('LLM Dependency OK')" 2>&1 || echo "Import check — verify after docker rebuild"</automated>
  </verify>
  <done>
    - llm_analysis_service содержит PyJWT в requirements.txt
    - AuthSettings в llm_analysis_service читает JWT_SECRET и JWT_ALGORITHM
    - get_current_user dependency создан в llm_analysis_service
    - Все 4 analysis эндпоинта, chat эндпоинты и transcript эндпоинты требуют JWT
    - /api/ingress/auth/login доступен через nginx
  </done>
</task>

</tasks>

<verification>
- Все recording-эндпоинты (кроме PATCH status) требуют JWT
- PATCH /recordings/{id}/status не требует JWT (worker callback)
- GET /recordings/ фильтрует по user_id из JWT
- POST /recordings/upload берет user_id из JWT
- Все analysis, chat, transcript эндпоинты требуют JWT
- llm_analysis_service использует тот же JWT_SECRET что data_ingress
</verification>

<success_criteria>
- Неавторизованные запросы к /recordings/* и /analysis/* возвращают 401
- PATCH status продолжает работать без JWT
- Записи фильтруются по user_id из JWT токена
</success_criteria>

<output>
After completion, create `.planning/phases/10-jwt-authentication/10-02-SUMMARY.md`
</output>
